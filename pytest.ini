[pytest]
testpaths = tests
markers =
    e2e: end-to-end tests that launch a real browser
    provider_live: makes real calls to the configured HEALER_PROVIDER (needs --provider-live)
addopts = -p no:cacheprovider
